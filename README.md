# danamojo

Donation widget UI (HTML / CSS / JS) with India/US currency, grid/list views, gift flow, checkout, payment, and 80G tax benefit forms.

## Run locally

Open `index.html` in a browser, or serve the folder with any static server.

## Deploy

Ready for Vercel (includes `vercel.json`).
